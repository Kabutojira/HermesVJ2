# Runtime architecture

HermesVJ2 is split into three layers: `src/experience/` for engine systems, `src/worlds/` for typed chapter definitions and scene content, and `src/components/` + `src/app/` for overlay UI and local state.
