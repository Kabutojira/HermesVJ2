import type { WorldSceneProps } from '../../../registry';
import { SaturnOrbitWorld } from '../../../shared/SaturnOrbitWorld';
export function SaturnOrbitInsertionWorld(props: WorldSceneProps) { return <SaturnOrbitWorld {...props} shotIndex={0} />; }
