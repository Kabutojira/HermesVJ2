import type { WorldSceneProps } from '../../../registry';
import { SaturnOrbitWorld } from '../../../shared/SaturnOrbitWorld';
export function SaturnRingsideDescentWorld(props: WorldSceneProps) { return <SaturnOrbitWorld {...props} shotIndex={1} />; }
