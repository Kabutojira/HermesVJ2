import type { WorldSceneProps } from '../../../registry';
import { SaturnOrbitWorld } from '../../../shared/SaturnOrbitWorld';
export function SaturnOrbitalDawnWorld(props: WorldSceneProps) { return <SaturnOrbitWorld {...props} shotIndex={4} />; }
