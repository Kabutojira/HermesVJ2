import type { WorldSceneProps } from '../../../registry';
import { SaturnOrbitWorld } from '../../../shared/SaturnOrbitWorld';
export function SaturnNightsidePlasmaWorld(props: WorldSceneProps) { return <SaturnOrbitWorld {...props} shotIndex={2} />; }
